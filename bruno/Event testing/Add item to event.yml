info:
  name: Add item to event
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8000/api/v1/events/1/appointments/1
  headers:
    - name: Accept
      value: application/json
    - name: Authorization
      value: Bearer {{bearerToken}}
  body:
    type: json
    data: |-
      {
        "date": "2026-05-30T23:00:00.000000Z"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
