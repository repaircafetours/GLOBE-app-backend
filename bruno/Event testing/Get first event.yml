info:
  name: Get first event
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8000/api/v1/events/1
  headers:
    - name: Authorization
      value: Bearer {{bearerToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
