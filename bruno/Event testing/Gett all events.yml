info:
  name: Gett all events
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/api/v1/event
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
