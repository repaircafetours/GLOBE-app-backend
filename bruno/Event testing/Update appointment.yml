info:
  name: Update appointment
  type: http
  seq: 8

http:
  method: PATCH
  url: http://localhost:8000/api/v1/events/1/appointments/1
  headers:
    - name: Accept
      value: application/json
    - name: Authorization
      value: Bearer {{bearerToken}}
  body:
    type: json
    data: |-
      {
        "comment": "Batterie HS, doit ramener une nouvelle pour le prochain rendez vous"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
