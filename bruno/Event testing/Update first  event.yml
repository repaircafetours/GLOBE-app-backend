info:
  name: Update first  event
  type: http
  seq: 5

http:
  method: PATCH
  url: http://localhost:8000/api/v1/events/1
  body:
    type: json
    data: |-
      {
        "date": "2026-05-31T22:00:00.000000Z"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
