info:
  name: Get all volunteers
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8000/api/v1/volunteers
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
