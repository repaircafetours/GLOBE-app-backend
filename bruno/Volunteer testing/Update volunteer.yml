info:
  name: Update volunteer
  type: http
  seq: 6

http:
  method: PATCH
  url: http://localhost:8000/api/v1/volunteers/4
  body:
    type: json
    data: |-
      {
        "idHumHub": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
