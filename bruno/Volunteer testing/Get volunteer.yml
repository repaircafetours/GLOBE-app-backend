info:
  name: Get volunteer
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8000/api/v1/volunteers/4
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
