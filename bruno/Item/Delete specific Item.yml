info:
  name: Delete specific Item
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/items/1"
  headers:
    - name: Authorization
      value: Bearer {{bearerToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
