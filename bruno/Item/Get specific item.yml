info:
  name: Get specific item
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/items/1"
  headers:
    - name: Authorization
      value: Bearer {{bearerToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
