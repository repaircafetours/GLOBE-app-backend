info:
  name: Get specific item from visitor
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/visitors/1/items/0"
  headers:
    - name: Authorization
      value: Bearer {{bearerToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
