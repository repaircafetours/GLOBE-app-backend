info:
  name: Get All Items From Visitor
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/v1/visitors/1/items"
  headers:
    - name: Authorization
      value: Bearer {{bearerToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
