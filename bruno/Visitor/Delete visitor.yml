info:
  name: Delete visitor
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/visitors/1"
  headers:
    - name: Authorization
      value: Bearer {{bearerToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
