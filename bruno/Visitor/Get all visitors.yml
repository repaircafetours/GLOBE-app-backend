info:
  name: Get all visitors
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/v1/visitors"
  headers:
    - name: Authorization
      value: Bearer {{bearerToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
