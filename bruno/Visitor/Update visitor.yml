info:
  name: Update visitor
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/visitors/1"
  headers:
    - name: Authorization
      value: Bearer {{bearerToken}}
  body:
    type: json
    data: |-
      {
        "notification": false
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
