info:
  name: Add visitor by volunteer
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/v1/visitors"
  headers:
    - name: Authorization
      value: Bearer {{bearerToken}}
  body:
    type: json
    data: |-
      {
        "title": "homme",
        "name": "Chahid",
        "surname": "Younes",
        "zip_code": "370000",
        "city": "tours",
        "phone_number": "0600000000",
        "source": "tyhu",
        "notification": true,
        "email": "a@a.com"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
