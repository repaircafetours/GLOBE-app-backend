info:
  name: Replace volunteer roles
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8000/api/v1/volunteers/1/roles
  body:
    type: json
    data: |-
      {
        "role_ids": [1, 2]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
