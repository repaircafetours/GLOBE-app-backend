info:
  name: Check volunteer has role
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/volunteers/1/roles/check/1"
  headers:
    - name: Authorization
      value: Bearer {{bearerToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
