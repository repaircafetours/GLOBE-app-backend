info:
  name: Get volunteer roles
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/api/v1/volunteers/1/roles
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
