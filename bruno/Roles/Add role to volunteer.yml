info:
  name: Add role to volunteer
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/api/v1/volunteers/1/roles/1/add
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
