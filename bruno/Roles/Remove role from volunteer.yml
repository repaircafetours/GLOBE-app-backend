info:
  name: Remove role from volunteer
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/volunteers/1/roles/1"
  headers:
    - name: Authorization
      value: Bearer {{bearerToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
