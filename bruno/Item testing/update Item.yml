info:
  name: update Item
  type: http
  seq: 7

http:
  method: PATCH
  url: http://localhost:8000/api/v1/items/1
  body:
    type: json
    data: |-
      {
        "weight": 999
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
