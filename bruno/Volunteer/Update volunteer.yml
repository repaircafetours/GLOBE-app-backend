info:
  name: Update volunteer
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/api/v1/volunteers/4"
  body:
    type: json
    data: |-
      {
        "idHumHub": 1
      }
  headers:
    - name: Authorization
      value: Bearer {{bearerToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
