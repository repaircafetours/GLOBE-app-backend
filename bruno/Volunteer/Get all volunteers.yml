info:
  name: Get all volunteers
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/v1/volunteers"
  headers:
    - name: Authorization
      value: Bearer {{bearerToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
